#include<stdio.h>

int main()
{
	//double i;
	int count = 0;
	printf(" %d %d %d\n", count++, count++, count++);
	/*
	 * for(i=0; i!=10; i+=0.1)
		printf("%.1f\n", i);
	 */
	printf("%d\n", count);
	return 0;
}
